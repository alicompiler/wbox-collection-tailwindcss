export interface TableTheme {
    table: string;
    head: string;
    headRow: string;
    headCell: string;
    body: string;
    bodyRow: string;
    stripedRow: string;
    hoveredRow: string;
    bodyCell: string;
}

export interface Theme {
    table: TableTheme;
}
